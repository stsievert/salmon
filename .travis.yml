language: python
python: 3.7

services:
  - docker

before_install:
  - docker-compose up &
  - docker ps

script:
  - pip install -r frontend/requirements.txt
  - export SALMON_NO_AUTH=1; pytest tests
